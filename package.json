{
  "name": "@dofu-lab/simui-cli",
  "version": "1.0.0",
  "description": "CLI to add SimUI components to your Angular project",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "simui": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "tsc --project tsconfig.test.json && node --test .test-dist/utils/registry.spec.js",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build",
    "prepublish": "npm run build && npm pack && npm publish --dry-run",
    "publish-package": "npm publish --access public"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.1",
    "chalk": "^5.4.1",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsup": "^8.4.0",
    "typescript": "^5.8.3"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "angular",
    "simui",
    "spartan",
    "cli",
    "components"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dofu-lab/sim-cli"
  }
}
